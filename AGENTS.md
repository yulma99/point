# AGENTS.md — 학급 포인트 앱

## 프로젝트 개요
초등학교 교사가 교실 앞 컴퓨터·태블릿에서 사용하는 학급 포인트 관리 웹앱.
학생을 등록하고 포인트를 주고 빼며, 브라우저를 껐다 켜도 데이터가 남아야 한다.
서버 없이 `index.html`을 더블클릭해 실행하는 프론트엔드 전용 앱이며, 저장은 `localStorage`로 한다.
사용자는 코딩을 모르는 교사이므로, 결과는 항상 브라우저에서 바로 확인할 수 있어야 한다.
이 프로젝트는 `/goal`로 장시간 기능 추가 작업을 시키는 데 쓰인다. 아래 규칙은 그때도 그대로 적용된다.

## 실행·확인 방법
- 실행: `index.html`을 Chrome에서 연다. 빌드·설치·서버 실행은 없다.
- 기본 점검 시나리오(모든 작업 후 반복):
  1. 학생 3명 등록 → 2. 포인트 +1/+3/+5 및 차감 → 3. 새로고침 → 값이 유지되는지 확인 → 4. 학생 1명 삭제 → 새로고침 → 삭제가 유지되는지 확인
- 콘솔(F12)에 오류가 없어야 한다.
- 문법 검사가 필요하면 `node --check script.js` 만 사용한다. (node가 없으면 생략)
- Git이 있으면 기능 하나가 안정적으로 끝날 때마다 한국어 메시지로 커밋한다. 예: `git commit -m "포인트 사유 기록 기능 추가"`

## 기술 범위
- HTML, CSS, JavaScript(ES2015+) 순수 파일만 사용한다.
- 외부 라이브러리, CDN, npm, 빌드 도구, 프레임워크, 서버, 백엔드 API를 사용하지 않는다.
- 데이터 저장은 `localStorage`만 사용한다. 쿠키·IndexedDB·외부 저장소는 쓰지 않는다.
- 차트가 필요하면 CSS 막대(div 너비)로 그린다. 차트 라이브러리는 쓰지 않는다.

## 파일 구조 (이 구조를 유지)
```
index.html   화면 구조. 인라인 <script>·<style> 금지
style.css    모든 스타일
script.js    앱 로직 (화면 렌더링, 이벤트, 계산)
storage.js   localStorage 읽기·쓰기 함수만 (loadState / saveState)
```
- 새 파일이 필요하면 만들기 전에 이유를 설명하고 사용자에게 확인한다.

## 데이터 형식
`localStorage` 키는 `classPoint` 하나만 쓴다. 저장 구조는 아래를 따르고, 필드를 추가할 수는 있지만 기존 필드 이름은 바꾸지 않는다.
```js
{
  version: 1,
  students: [
    { id: "s_1712345678", number: 1, name: "연습생1", points: 5 }
  ],
  history: [
    { id: "h_1712345690", studentId: "s_1712345678",
      delta: 3, reason: "발표", at: "2026-09-07T09:30:00" }
  ]
}
```
- `id`는 `"s_" + Date.now()` 형식의 문자열. 배열 인덱스를 id로 쓰지 않는다.
- 구조를 바꿀 때는 `version`을 올리고, 이전 버전 데이터를 읽어 변환하는 코드를 `storage.js`에 넣는다. 기존 사용자 데이터가 사라지면 안 된다.
- 학생 이름 예시는 "연습생1", "연습생2"처럼 가상 이름만 쓴다. 실제 사람 이름을 코드에 넣지 않는다.

## 코딩 규칙
- UI 문구는 모두 한국어로 쓴다.
- 변수·함수 이름은 영어 camelCase, 파일 상단에 `'use strict';`
- 화면 갱신은 `render()` 하나로 모아 상태가 바뀔 때마다 호출한다. DOM을 부분적으로 직접 고치지 않는다.
- 상태를 바꾸는 함수는 항상 마지막에 `saveState()`를 호출한다.
- `index.html`에 `onclick=` 속성을 쓰지 않는다. 이벤트는 `script.js`에서 붙인다.
- 삭제·초기화처럼 되돌릴 수 없는 동작은 `confirm()`으로 한 번 확인한다.
- 버튼 높이 최소 48px, 본문 글자 최소 18px. 화면 너비 768px(태블릿)과 1280px(교실 PC)에서 모두 깨지지 않게 한다.
- 학생 카드는 CSS Grid로 배치한다.

## 경계
✅ 항상 한다
- 수정 후 위 "기본 점검 시나리오"를 실제로 수행한다.
- 기존 기능(등록·수정·삭제·지급·차감·저장 유지)이 그대로 동작하는지 확인한다.
- 기능 하나가 끝날 때마다 무엇을 했는지 한 줄로 기록하고, 전체 작업이 끝나면 파일별 변경 내용을 요약한다.

⚠️ 먼저 묻는다
- 파일을 추가·삭제하거나 파일 구조를 바꿀 때
- `localStorage` 저장 구조(`version`이 바뀌는 변경)를 바꿀 때
- 저장된 데이터를 지우거나 초기화하는 코드를 추가할 때

🚫 하지 않는다
- 외부 라이브러리·CDN·패키지 설치·서버 코드·네트워크 요청 추가
- 프로젝트 폴더 밖의 파일 읽기·수정
- 사용자가 요청하지 않은 기능 추가
- 실제 학생 이름·학번 등 개인정보를 코드나 예시 데이터에 넣기
- 기존 사용자 데이터가 사라지는 변경

## 완료 기준
- 콘솔 오류 0개
- "기본 점검 시나리오" 4단계 모두 통과
- 요청받은 기능 각각을 `index.html`에서 직접 눌러 확인
- 위 항목을 직접 확인한 뒤에만 "완료"라고 보고한다. `/goal` 작업에서도 이 기준을 충족해야 완료 처리한다.